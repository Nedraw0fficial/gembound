# Tim, voici les cmd bash pour le serveur oracle :

sudo systemctl status gembound    # vérifier que ça tourne
sudo journalctl -u gembound -f    # voir les logs en direct
sudo systemctl restart gembound   # relancer après une mise à jour du code

# et quand on update le projet :

(connection ssh dans powershell, je te passerais la clef)
ssh -i "CHEMIN_SSH_CLEF" ubuntu@89.168.61.135
cd gembound
git pull
sudo systemctl restart gembound

et si update des requirements, juste apres le pull :

source .venv/bin/activate
pip install -r requirements.txt

# setup

git clone https://github.com/Nedraw0fficial/gembound.git
cd gembound
python -m venv .venv
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python main.py